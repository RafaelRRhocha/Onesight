import type { FC } from 'react';
import Read from '../components/Read';

interface readProps {}

const read: FC<readProps> = ({}) => {
  return (
    <>
      <Read />
    </>
  );
}

export default read;